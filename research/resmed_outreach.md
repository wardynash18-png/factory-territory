# ResMed outreach — account_id A003

# DO NOT PROSPECT YET

The seed's specific SIGNAL — `Live reqs seeking engineers to build agentic AI workflows that are traceable/auditable in regulated environments` — is **`NO SIGNAL FOUND`** in this research pass. ResMed's public careers portal (`https://careers.resmed.com/`) returns 71 jobs in San Diego via Glassdoor, 36 via Indeed — none matching "agentic AI", "auditable", or "traceable" ($\rightarrow$ seed claim does not surface).

## Verified-only touch points

Until a matching req surfaces, the **only** verified touch point is CTO **Urvashi Tyagi** (LinkedIn `https://www.linkedin.com/in/urvashityagi`, surfaced in current search 2024-10). Bloomberg Mark (`https://www.bloomberg.com/profile/person/22688752`) lists her as **Former CTO** — the title and Tenure have to be verified *today* once before outreach. Reaching her before verification is high-rep, low-signal risk.

## What we are NOT doing

- **No `_paused` slot-block files.** The seed claim is not retrievable; we are not writing placeholder emails until the SIGNAL is found.
- **No CISO/CIO outreach.** Same reason: neither role is publicly named; the slot is `SLOT ONLY`. Sending on a slot-only footprint would be a fishing trip.
- **No 5-slot templates.** The framework's strength is calibrated evidence per slot; without evidence, the calibration is theater.

## When to resume prospecting

Resume when **any** of the following surfaces:

1. A live Req at `careers.resmed.com` matching `agentic AI` / `auditable` / `traceable` keyword (re-search monthly).
2. A named CIO or CISO at ResMed (LinkedIn people-search or 10-K).
3. Tyagi's *current* title — confirm via Req release or her own contact update.

When one of these surfaces, write `research/resmed.md SED VERSION 2` with the new SIGNAL, then draft slots.
